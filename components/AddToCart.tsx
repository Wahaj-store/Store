'use client';import {useState} from 'react';
export default function AddToCart({product,variantId}:{product:any,variantId?:string}){const [done,setDone]=useState(false);function add(){if(product.variants?.length && !variantId)return;const variant=variantId?product.variants?.find((x:any)=>x.id===variantId):null;const available=variant?variant.stock:product.stock;if(!available)return;const price=variant?.price!=null?Number(variant.price):Number(product.price);const cart=JSON.parse(localStorage.getItem('wahaj_cart')||'[]');const key=variant?`${product.id}:${variant.id}`:product.id;const i=cart.findIndex((x:any)=>`${x.productId}:${x.variantId||''}`===key);if(i>=0)cart[i].quantity=Math.min(cart[i].quantity+1,available);else cart.push({productId:product.id,variantId:variant?.id||undefined,variantName:variant?.name,variantValue:variant?.value,name:product.name,price,image:variant?.imageUrl||product.images?.[0]?.url||'',quantity:1,maxStock:available});localStorage.setItem('wahaj_cart',JSON.stringify(cart));window.dispatchEvent(new Event('wahaj-cart-change'));setDone(true);setTimeout(()=>setDone(false),1400)}const disabled=product.variants?.length ? !variantId || !(product.variants?.find((x:any)=>x.id===variantId)?.stock) : !product.stock;return <button disabled={disabled} onClick={add} className="btn btn-gold w-full" aria-live="polite">{done?'تمت الإضافة ✓':product.variants?.length&&!variantId?'اختاري الخيارات أولاً':'أضيفي إلى السلة'}</button>}
