'use client'; import {useState} from 'react';
export default function BackInStockForm({productId}:{productId:string}){const[email,setEmail]=useState('');const[phone,setPhone]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(e:any){e.preventDefault();setBusy(true);setMsg('');try{const r=await fetch(`/api/products/${productId}/back-in-stock`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,phone})});const j=await r.json();if(!r.ok)throw Error(j.error);setMsg(j.message);setEmail('');setPhone('')}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}return <form onSubmit={submit} className="mt-5 lux-card p-5"><b>أبلغيني عند التوفر</b><p className="muted mt-1 text-sm">سنحتفظ ببيانات التنبيه لهذا المنتج فقط.</p><div className="mt-4 grid gap-2 md:grid-cols-2"><input className="input" type="email" placeholder="البريد الإلكتروني" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" placeholder="رقم الهاتف" value={phone} onChange={e=>setPhone(e.target.value)}/></div><button disabled={busy} className="btn btn-gold mt-3">{busy?'جارٍ الحفظ…':'تفعيل التنبيه'}</button>{msg&&<p className="mt-3 text-sm">{msg}</p>}</form>}
