'use client';import {useState} from 'react';export default function ReviewForm({productId}:{productId:string}){const[rating,setRating]=useState(5);const[text,setText]=useState('');const[msg,setMsg]=useState('');async function submit(){const r=await fetch('/api/reviews',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({productId,rating,text})});const j=await r.json();if(!r.ok)return setMsg(j.error);setText('');setMsg('تم إرسال تقييمك للمراجعة والنشر.')}return <div className="lux-card mt-6 p-5"><h3 className="font-semibold">أضيفي تقييمك</h3><div className="mt-3 flex gap-1">{[1,2,3,4,5].map(n=><button key={n} onClick={()=>setRating(n)} className={n<=rating?'gold':'muted'}>★</button>)}</div><textarea className="input mt-3 min-h-24" placeholder="اكتبي تجربتك..." value={text} onChange={e=>setText(e.target.value)}/><button className="btn btn-gold mt-3" onClick={submit}>إرسال التقييم</button>{msg&&<p className="mt-2 text-sm muted">{msg}</p>}</div>}
