'use client';import {Heart} from 'lucide-react';import {useState} from 'react';export default function WishlistButton({productId}:{productId:string}){const[saved,setSaved]=useState(false);const[busy,setBusy]=useState(false);async function toggle(){setBusy(true);const r=await fetch('/api/customer/wishlist',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({productId})});const j=await r.json();if(r.ok)setSaved(j.saved);else alert(j.error||'سجلي الدخول أولاً');setBusy(false)}return <button aria-label="إضافة للمفضلة" disabled={busy} onClick={toggle} className={`rounded-full border p-3 bg-[var(--card)] ${saved?'gold':''}`}><Heart size={18} fill={saved?'currentColor':'none'}/></button>}
