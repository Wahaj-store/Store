'use client'
import {useEffect,useState} from 'react'; export default function Orders(){const[o,setO]=useState<any[]>([]);useEffect(()=>{fetch('/api/admin/orders').then(r=>r.json()).then(setO)},[]);return <main className="container py-10"><a href="/admin" className="gold text-sm">‹ لوحة التحكم</a><h1 className="mt-5 text-3xl font-semibold">الطلبات</h1><div className="mt-8 grid gap-3">{o.map(x=><div className="lux-card p-5" key={x.id}><div className="flex justify-between"><b>{x.number}</b><span>{x.status}</span></div><p className="mt-2 text-sm muted">{x.customer?.name} • {x.customer?.phone}</p><p className="mt-3">{Number(x.total).toLocaleString('ar-EG')} ج.م • {x.paymentMethod}</p></div>)}{!o.length&&<div className="lux-card p-8 text-center muted">لا توجد طلبات حتى الآن.</div>}</div></main>}
