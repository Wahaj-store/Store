'use client'
import {useEffect,useState} from 'react'; export default function Settings(){const[s,setS]=useState<any>({});useEffect(()=>{fetch('/api/admin/config').then(r=>r.json()).then(x=>setS(x.settings||{}))},[]);return <main className="container py-10"><a href="/admin" className="gold text-sm">‹ لوحة التحكم</a><h1 className="mt-5 text-3xl font-semibold">الإعدادات</h1><div className="lux-card mt-8 p-6"><label className="text-sm">رقم واتساب<input className="input mt-2" value={s.whatsapp||''} onChange={e=>setS({...s,whatsapp:e.target.value})}/></label><label className="mt-5 block text-sm">شريط الإعلان<input className="input mt-2" value={s.announcement||''} onChange={e=>setS({...s,announcement:e.target.value})}/></label><p className="mt-5 text-xs muted">يمكن توسيع نفس الواجهة لبقية إعدادات المتجر.</p></div></main>}
