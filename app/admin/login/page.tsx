'use client';import {useState} from 'react';import {Eye,EyeOff,ShieldCheck} from 'lucide-react';import {useRouter} from 'next/navigation';
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false);const r=useRouter();async function submit(e:any){e.preventDefault();setBusy(true);setError('');try{const x=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const j=await x.json().catch(()=>({}));if(x.ok)r.replace('/admin');else setError(j.error||'تعذر الدخول')}catch{setError('تعذر الاتصال بالخادم')}finally{setBusy(false)}}return <main className="min-h-screen grid place-items-center bg-[var(--bg)] p-5"><form onSubmit={submit} className="lux-card w-full max-w-md p-7 shadow-xl"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-full bg-[var(--gold)] text-[#171513]"><ShieldCheck size={21}/></div><div><div className="text-2xl font-semibold">وَهَج</div><p className="muted text-xs">لوحة الإدارة الآمنة</p></div></div><h1 className="mt-8 text-2xl font-semibold">تسجيل الدخول</h1><p className="mt-2 text-sm muted">استخدمي بيانات الإدارة التي تم إعدادها في بيئة التشغيل.</p><label className="mt-7 block text-sm">البريد الإلكتروني<input required autoComplete="username" className="input mt-2" type="email" value={email} onChange={e=>setEmail(e.target.value)} dir="ltr"/></label><label className="mt-4 block text-sm">كلمة المرور<div className="relative mt-2"><input required minLength={8} autoComplete="current-password" className="input pe-11" type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} dir="ltr"/><button type="button" onClick={()=>setShow(!show)} className="absolute inset-y-0 start-3" aria-label={show?'إخفاء كلمة المرور':'إظهار كلمة المرور'}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label>{error&&<p className="mt-4 rounded border border-red-300 bg-red-50 p-3 text-sm text-red-700" role="alert">{error}</p>}<button disabled={busy} className="btn btn-gold mt-6 w-full">{busy?'جارٍ التحقق…':'دخول لوحة التحكم'}</button></form></main>}
