'use client'
import {useEffect,useState} from 'react'; export default function Products(){const[p,setP]=useState<any[]>([]);useEffect(()=>{fetch('/api/admin/products').then(r=>r.json()).then(setP)},[]);return <main className="container py-10"><a href="/admin" className="gold text-sm">‹ لوحة التحكم</a><h1 className="mt-5 text-3xl font-semibold">المنتجات</h1><p className="mt-2 muted">إدارة الأسعار والمخزون والحالة والصور.</p><div className="mt-8 overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b hairline"><th className="p-3 text-start">المنتج</th><th className="p-3">السعر</th><th className="p-3">المخزون</th><th className="p-3">الحالة</th></tr></thead><tbody>{p.map(x=><tr className="border-b hairline" key={x.id}><td className="p-3">{x.name}<div className="text-xs muted">{x.sku}</div></td><td className="p-3">{Number(x.price).toLocaleString('ar-EG')} ج.م</td><td className="p-3">{x.stock}</td><td className="p-3">{x.status}</td></tr>)}</tbody></table>{!p.length&&<p className="p-8 text-center muted">لا توجد منتجات.</p>}</div></main>}
