import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {requireUser} from '@/lib/auth';
export async function GET(){const u=await requireUser(['OWNER','ADMIN','MANAGER','EDITOR','VIEWER']);if(!u)return NextResponse.json({error:'غير مصرح'},{status:401});return NextResponse.json(await prisma.category.findMany({orderBy:{sortOrder:'asc'}}))}
export async function POST(req:Request){const u=await requireUser(['OWNER','ADMIN','MANAGER','EDITOR']);if(!u)return NextResponse.json({error:'غير مصرح'},{status:403});const b=await req.json();if(!b.name||!b.slug)return NextResponse.json({error:'الاسم والرابط مطلوبان'},{status:400});return NextResponse.json(await prisma.category.create({data:{name:b.name,slug:b.slug,description:b.description||null,imageUrl:b.imageUrl||null,sortOrder:Number(b.sortOrder||0)}}),{status:201})}
export async function PUT(req:Request){const u=await requireUser(['OWNER','ADMIN','MANAGER','EDITOR']);if(!u)return NextResponse.json({error:'غير مصرح'},{status:403});const b=await req.json();return NextResponse.json(await prisma.category.update({where:{id:b.id},data:{name:b.name,slug:b.slug,description:b.description||null,imageUrl:b.imageUrl||null,sortOrder:Number(b.sortOrder||0),active:b.active!==false}}))}
export async function DELETE(req:Request){const u=await requireUser(['OWNER','ADMIN','MANAGER']);if(!u)return NextResponse.json({error:'غير مصرح'},{status:403});const {id}=await req.json();const refs=await prisma.product.count({where:{categoryId:id}});if(refs>0)return NextResponse.json({error:'لا يمكن حذف تصنيف يحتوي على منتجات. انقلي المنتجات أو عطّلي التصنيف أولًا.'},{status:409});return NextResponse.json(await prisma.category.delete({where:{id}}))}
