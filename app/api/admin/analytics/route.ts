import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {requireUser} from '@/lib/auth';
export async function GET(){const u=await requireUser(['OWNER','ADMIN','MANAGER','VIEWER']);if(!u)return NextResponse.json({error:'غير مصرح'},{status:401});const [orders,products,customers,reviews,lowStock,recent]=await Promise.all([prisma.order.count(),prisma.product.count(),prisma.customer.count(),prisma.review.count(),prisma.product.count({where:{stock:{lte:5}}}),prisma.order.findMany({select:{total:true,status:true,createdAt:true},orderBy:{createdAt:'desc'},take:12})]);const agg=await prisma.order.aggregate({where:{status:{not:'CANCELLED'}},_sum:{total:true},_avg:{total:true}});return NextResponse.json({orders,products,customers,reviews,lowStock,revenue:Number(agg._sum.total||0),aov:Number(agg._avg.total||0),recent})}
