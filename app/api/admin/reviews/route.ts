import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {requireUser} from '@/lib/auth';
export async function GET(){const u=await requireUser(['OWNER','ADMIN','MANAGER','EDITOR','VIEWER']);if(!u)return NextResponse.json({error:'غير مصرح'},{status:401});return NextResponse.json(await prisma.review.findMany({include:{product:{select:{id:true,name:true,slug:true}},customer:{select:{id:true,name:true,phone:true,email:true}}},orderBy:{createdAt:'desc'}}))}
export async function PUT(req:Request){const u=await requireUser(['OWNER','ADMIN','MANAGER','EDITOR']);if(!u)return NextResponse.json({error:'غير مصرح'},{status:403});const b=await req.json();return NextResponse.json(await prisma.review.update({where:{id:b.id},data:{approved:Boolean(b.approved),verified:Boolean(b.verified)}}))}
export async function DELETE(req:Request){const u=await requireUser(['OWNER','ADMIN','MANAGER']);if(!u)return NextResponse.json({error:'غير مصرح'},{status:403});await prisma.review.delete({where:{id:(await req.json()).id}});return NextResponse.json({ok:true})}
